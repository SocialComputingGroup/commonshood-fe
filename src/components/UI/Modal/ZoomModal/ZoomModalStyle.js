const zoomModalStyle = theme => ({
    root: {
        width: '92%',
        padding: theme.spacing(1) /2,
        margin: theme.spacing(2)
    },
    content:{
        padding: theme.spacing(1)
    },
    footer: {
        display: 'flex',
        justifyContent: 'center'
    }
});

export default zoomModalStyle;