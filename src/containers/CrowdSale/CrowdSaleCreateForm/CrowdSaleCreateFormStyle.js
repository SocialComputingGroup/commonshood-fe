const crowdSaleCreateFormStyle = theme => ({
    fieldContainer: {
        display: 'flex',
        alignItems:'left',
        margin: theme.spacing(1)
    },
    buttons: {
        backgroundColor: theme.palette.secondary.main,
        color: theme.palette.grey[200],
        margin: theme.spacing(1)
    },

});


export default crowdSaleCreateFormStyle;
