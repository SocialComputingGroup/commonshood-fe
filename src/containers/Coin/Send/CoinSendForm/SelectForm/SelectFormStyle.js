const selectFormStyle = theme => ({
    buttonsContainer: {
        padding: theme.spacing(1)
    },
    buttons: {
        backgroundColor: theme.palette.secondary.main,
        color: theme.palette.grey[200],
    },
    symbolClass: {
        padding: theme.spacing(1)
    }
});

export default selectFormStyle;