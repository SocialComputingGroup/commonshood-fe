const helpModalStyle = theme => ({
    buttons: {
        backgroundColor: theme.palette.secondary.main,
        color: theme.palette.grey[200],
        margin: theme.spacing(1)
    },
});
export default helpModalStyle;